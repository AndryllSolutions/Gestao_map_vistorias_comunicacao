<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Histórico de Ações</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
    }
    .container-box {
      background: #fff;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.05);
    }
  </style>
</head>
<body class="bg-light p-3 p-md-4">

  <div class="container container-box">
    <header class="d-flex flex-column flex-md-row justify-content-between align-items-start align-items-md-center mb-4">
      <div>
        <h3><i class="bi bi-clock-history"></i> Histórico de Ações</h3>
        <p class="text-muted mb-0">Acompanhe todas as ações realizadas no sistema (apenas admins).</p>
      </div>
      <a href="/dashboard" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left"></i> Voltar ao Dashboard
      </a>
    </header>
<form class="row g-2 mb-4" method="get">
  <div class="col-md-3">
    <input type="date" name="inicio" class="form-control" value="{{ request.args.inicio }}">
  </div>
  <div class="col-md-3">
    <input type="date" name="fim" class="form-control" value="{{ request.args.fim }}">
  </div>
  <div class="col-md-3">
    <select name="tipo" class="form-select">
      <option value="">Todas as ações</option>
      <option value="criação" {% if request.args.tipo == 'criação' %}selected{% endif %}>Criação</option>
      <option value="edição" {% if request.args.tipo == 'edição' %}selected{% endif %}>Edição</option>
      <option value="exclusão" {% if request.args.tipo == 'exclusão' %}selected{% endif %}>Exclusão</option>
    </select>
  </div>
  <div class="col-md-3 d-grid">
    <button class="btn btn-dark" type="submit"><i class="bi bi-funnel"></i> Filtrar</button>
  </div>
</form>

    <div class="table-responsive">
      <table class="table table-hover align-middle">
        <thead class="table-dark text-center">
          <tr>
            <th>#</th>
            <th>Usuário</th>
            <th>Ação</th>
            <th>Entidade</th>
            <th>ID Alvo</th>
            <th>Descrição</th>
            <th>Data e Hora</th>
          </tr>
        </thead>
        <tbody class="text-center">
          {% for acao in acoes %}
          <tr>
            <td>{{ acao.id }}</td>
            <td>{{ acao.usuario.email }}</td>
            <td>
              {% if acao.tipo_acao == "criação" %}
                <span class="badge bg-success"><i class="bi bi-plus-circle"></i> {{ acao.tipo_acao }}</span>
              {% elif acao.tipo_acao == "edição" %}
                <span class="badge bg-warning text-dark"><i class="bi bi-pencil-square"></i> {{ acao.tipo_acao }}</span>
              {% elif acao.tipo_acao == "exclusão" %}
                <span class="badge bg-danger"><i class="bi bi-trash3"></i> {{ acao.tipo_acao }}</span>
              {% else %}
                {{ acao.tipo_acao }}
              {% endif %}
            </td>
            <td>{{ acao.entidade }}</td>
            <td>{{ acao.entidade_id }}</td>
            <td class="text-start">{{ acao.observacao }}</td>
            <td>{{ acao.data_hora.strftime('%d/%m/%Y %H:%M:%S') }}</td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <title>Editar Agendamento</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
</head>
<body class="bg-light p-3">
  <div class="container bg-white p-4 rounded shadow">
    <h3 class="mb-4"><i class="bi bi-pencil-square"></i> Editar Agendamento</h3>

    <form method="POST">
      <div class="row">
        <div class="col-md-6 mb-3">
          <label>Nome do Morador</label>
          <input type="text" name="nome_morador" class="form-control" value="{{ agendamento.nome_morador }}" required />
        </div>
        <div class="col-md-6 mb-3">
          <label>Celular</label>
          <input type="tel" name="celular" class="form-control" value="{{ agendamento.celular }}" required />
        </div>

        <div class="col-md-6 mb-3">
          <label>Endereço</label>
          <input type="text" name="endereco" class="form-control" value="{{ agendamento.endereco }}" required />
        </div>
        <div class="col-md-3 mb-3">
          <label>Bairro</label>
          <input type="text" name="bairro" class="form-control" value="{{ agendamento.bairro }}" required />
        </div>
        <div class="col-md-3 mb-3">
          <label>Cidade</label>
          <input type="text" name="cidade" class="form-control" value="{{ agendamento.cidade }}" required />
        </div>

        <div class="col-md-6 mb-3">
          <label>Data da Vistoria</label>
          <input type="date" name="data_agendada" class="form-control" value="{{ agendamento.data_agendada }}" required />
        </div>
        <div class="col-md-6 mb-3">
          <label>Hora da Vistoria</label>
          <input type="time" name="hora_agendada" class="form-control" value="{{ agendamento.hora_agendada }}" required />
        </div>

        <div class="col-12 mb-3">
          <label>Observações</label>
          <textarea name="observacoes" class="form-control">{{ agendamento.observacoes }}</textarea>
        </div>
      </div>

      <div class="d-flex justify-content-between">
        <a href="/agendamentos" class="btn btn-outline-secondary">
          <i class="bi bi-arrow-left"></i> Voltar
        </a>
        <button type="submit" class="btn btn-warning">
          <i class="bi bi-save"></i> Atualizar
        </button>
      </div>
    </form>
  </div>
</body>
</html>

<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <title>Vistorias Registradas</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f8f9fa;
      }
      .container-box {
        background-color: white;
        padding: 20px;
        border-radius: 12px;
        box-shadow: 0 0 8px rgba(0, 0, 0, 0.05);
      }
    </style>
  </head>
  <body class="bg-light p-3 p-md-4">
    <div class="container container-box">
      <!-- Cabeçalho -->
      <header
        class="d-flex flex-column flex-md-row justify-content-between align-items-start align-items-md-center gap-2 mb-4"
      >
        <div>
          <h3 class="mb-1">
            <i class="bi bi-clipboard-data"></i> Registros de Vistorias
          </h3>
          <p class="text-muted small mb-0">
            Lista das vistorias cadastradas no sistema.
          </p>
        </div>
        <div class="d-flex flex-column flex-md-row gap-2 w-100 w-md-auto">
          <a href="/vistoria" class="btn btn-success w-100 w-md-auto">
            <i class="bi bi-plus-circle"></i> Nova Vistoria
          </a>
          <a
            href="/dashboard"
            class="btn btn-outline-secondary w-100 w-md-auto"
          >
            <i class="bi bi-arrow-left"></i> Voltar ao Dashboard
          </a>
        </div>
      </header>

      <!-- Campo de filtro -->
      <div class="mb-3">
        <input
          type="text"
          id="filtroVistoria"
          class="form-control"
          placeholder="🔍 Filtrar por bairro, rua, tipo ou observação..."
        />
      </div>

      <!-- Tabela -->
      <div class="table-responsive">
        <table
          class="table table-striped table-hover align-middle"
          id="tabelaVistorias"
        >
          <thead class="table-dark text-center">
            <tr>
              <th>ID</th>
              <th>Município</th>
              <th>Bairro</th>
              <th>Rua</th>
              <th>1ª Tentativa</th>
              <th>Tipo</th>
              <th>Observações</th>
              <th>Ações</th>
            </tr>
          </thead>
          <tbody class="text-center">
            {% for v in vistorias %}
            <tr>
              <td>{{ v.id }}</td>
              <td>{{ v.municipio }}</td>
              <td>{{ v.bairro }}</td>
              <td>{{ v.rua }}</td>
              <td>{{ v.data_1 }}<br /><small>{{ v.hora_1 }}</small></td>
              <td>{{ v.tipo_imovel }}</td>
              <td>{{ v.observacoes }}</td>
              <!-- Dentro da <td> Ações -->
              <td>
                {% if cargo == 'admin' %}
                <div class="dropdown">
                  <button
                    class="btn btn-outline-dark btn-sm dropdown-toggle"
                    type="button"
                    data-bs-toggle="dropdown"
                    aria-expanded="false"
                  >
                    Ações
                  </button>
                  <ul class="dropdown-menu">
                    <li>
                      <a
                        class="dropdown-item"
                        href="/vistoria/laudo/{{ v.id }}"
                      >
                        <i class="bi bi-file-earmark-pdf"></i> Laudo
                      </a>
                    </li>
                    <li>
                      <a
                        class="dropdown-item"
                        href="/vistoria/editar/{{ v.id }}"
                      >
                        <i class="bi bi-pencil-square"></i> Editar
                      </a>
                    </li>
                    <li>
                      <form
                        action="{{ url_for('excluir_vistoria', id=v.id) }}"
                        method="post"
                        onsubmit="return confirm('Tem certeza que deseja excluir esta vistoria?')"
                      >
                        <button type="submit" class="dropdown-item text-danger">
                          <i class="bi bi-trash"></i> Excluir
                        </button>
                      </form>
                    </li>

                    <li><hr class="dropdown-divider" /></li>
                    <li>
                      <a
                        class="dropdown-item"
                        href="/exportar/vistoria/{{ v.id }}/pdf"
                      >
                        <i class="bi bi-filetype-pdf"></i> Exportar PDF
                      </a>
                    </li>
                    <li>
                      <a
                        class="dropdown-item"
                        href="/exportar/vistoria/{{ v.id }}/excel"
                      >
                        <i class="bi bi-file-earmark-excel"></i> Exportar Excel
                      </a>
                    </li>
                  </ul>
                </div>
                {% else %}
                <a
                  class="btn btn-outline-primary btn-sm"
                  href="/vistoria/laudo/{{ v.id }}"
                >
                  <i class="bi bi-file-earmark-pdf"></i> Laudo
                </a>
                {% endif %}
              </td>
            </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
    </div>

    <!-- Scripts -->
    <script>
      document
        .getElementById("filtroVistoria")
        .addEventListener("keyup", function () {
          const filtro = this.value.toLowerCase();
          const linhas = document.querySelectorAll("#tabelaVistorias tbody tr");

          linhas.forEach((linha) => {
            const textoLinha = linha.textContent.toLowerCase();
            linha.style.display = textoLinha.includes(filtro) ? "" : "none";
          });
        });
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
